# 🏗️ Architecture Diagram - Multi-Timezone Display

## System Architecture & Data Flow

```
┌──────────────────────────────────────────────────────────────────────┐
│                    ODOO MEETING ROOMS MODULE                         │
└──────────────────────────────────────────────────────────────────────┘
                                    │
                        ┌───────────┴───────────┐
                        ▼                       ▼
                ┌──────────────────┐    ┌──────────────────┐
                │  meeting.event   │    │  meeting.rooms   │
                │   (Master)       │    │   (Detail)       │
                └────────┬─────────┘    └────────┬─────────┘
                         │                       │
                         │◄──────Related─────────┘
                         │
            ┌────────────┴────────────┐
            ▼                         ▼
    ┌──────────────────┐     ┌────────────────────┐
    │ FIELD: start_date │    │ FIELD: room_location│
    │       end_date    │    │         ids         │
    │   room_location_  │    │   host_user_id     │
    │      ids          │    │  virtual_room_id   │
    │   host_user_id    │    │    zoom_link       │
    │ virtual_room_id   │    └────────────────────┘
    │   zoom_link       │
    └────────┬──────────┘
             │
      [TRIGGERS CALCULATION]
             │
      @api.depends('start_date', 'end_date', 
                   'room_location_ids', 'host_user_id',
                   'virtual_room_id', 'zoom_link')
             │
             ▼
    ┌──────────────────────────────────────────────┐
    │  _compute_multi_timezone_display()           │
    │                                              │
    │  Purpose: Generate HTML timezone table       │
    │                                              │
    │  Logic:                                      │
    │  1. For each room_location:                  │
    │     - Get room timezone (room.tz)            │
    │     - Convert start_date to room timezone    │
    │     - Convert end_date to room timezone      │
    │     - Format as "HH:MM - HH:MM (TZ)"        │
    │                                              │
    │  2. If virtual meeting exists:               │
    │     - Use host_user_id.tz                    │
    │     - Show as "🎥 {provider} (Host)"        │
    │                                              │
    │  3. Generate HTML table with emoji & styling │
    │                                              │
    │  Result: Stored in multi_timezone_display    │
    └──────────┬───────────────────────────────────┘
               │
         [FIELD STORED IN DB]
               │
    ┌──────────┴──────────────────────────────────────┐
    │                                                  │
    ▼                                                  ▼
┌────────────────────────┐              ┌──────────────────────────┐
│  FORM VIEWS            │              │  ACTIVITY NOTIFICATIONS  │
├────────────────────────┤              ├──────────────────────────┤
│ meeting_event form     │              │  mail.activity           │
│ ─ Separator            │              │  ─ Note field            │
│ ─ Field displayed      │              │    includes:             │
│                        │              │    - Schedule table      │
│ meeting.rooms form     │              │    - multi_timezone_    │
│ ─ Separator            │              │      display HTML        │
│ ─ Related field        │              │    - Join button         │
│   (from parent event)  │              │                          │
└────────────────────────┘              └──────────────────────────┘
         │                                            │
         │                                            │
  ┌──────▼────────────────────────────┬───────────────▼──────────┐
  │                                    │                          │
  ▼                                    ▼                          ▼
┌─────────────────────┐       ┌──────────────────┐    ┌────────────────┐
│ ADMIN/HOST sees:    │       │ STAFF/FIELD sees │    │ ATTENDEE sees: │
├─────────────────────┤       ├──────────────────┤    ├────────────────┤
│ • Form with table   │       │ • Room form with │    │ • Activity     │
│ • Can plan better   │       │   table          │    │   notification │
│ • Send invites      │       │ • Understands    │    │ • All times    │
│   confidently       │       │   local times    │    │   at a glance  │
│ • See conflicts     │       │ • No confusion   │    │ • Click join   │
│                     │       │   about times    │    │   if available │
└─────────────────────┘       └──────────────────┘    └────────────────┘
```

---

## Timezone Conversion Process (Deep Dive)

```
INPUT: start_date = "2024-02-08 02:00:00" (UTC stored in database)
       room = "Ruangan Jakarta" with tz = "Asia/Jakarta"
       room = "Ruangan Makassar" with tz = "Asia/Makassar"
       host.tz = "Asia/Jakarta"

┌─────────────────────────────────────────────────┐
│  STEP 1: Localize to UTC (ensure UTC status)    │
├─────────────────────────────────────────────────┤
│  utc_dt = pytz.utc.localize("2024-02-08 02:00")│
│  Result: 2024-02-08 02:00:00+00:00              │
└────────────────┬────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────┐
│  STEP 2: Convert to Jakarta timezone (UTC+7)    │
├─────────────────────────────────────────────────┤
│  tz_jakarta = pytz.timezone('Asia/Jakarta')     │
│  local_jakarta = utc_dt.astimezone(tz_jakarta)  │
│  Result: 2024-02-08 09:00:00+07:00              │
│  Display: "09:00"                               │
└────────────────┬────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────┐
│  STEP 3: Convert to Makassar timezone (UTC+8)   │
├─────────────────────────────────────────────────┤
│  tz_makassar = pytz.timezone('Asia/Makassar')   │
│  local_makassar = utc_dt.astimezone(tz_makassar)│
│  Result: 2024-02-08 10:00:00+08:00              │
│  Display: "10:00"                               │
└────────────────┬────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────┐
│  STEP 4: Generate HTML table with results       │
├─────────────────────────────────────────────────┤
│  <table>                                        │
│    <tr>                                         │
│      <td>🏢 Ruangan Jakarta</td>               │
│      <td>09:00 - 11:00 (Asia/Jakarta)</td>     │
│    </tr>                                        │
│    <tr>                                         │
│      <td>🏢 Ruangan Makassar</td>              │
│      <td>10:00 - 12:00 (Asia/Makassar)</td>    │
│    </tr>                                        │
│    <tr>                                         │
│      <td>🎥 Zoom (Host)</td>                   │
│      <td>09:00 - 11:00 (Asia/Jakarta)</td>     │
│    </tr>                                        │
│  </table>                                       │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
        STORED IN DATABASE
        (multi_timezone_display)
```

---

## Field Dependencies & Recalculation

```
                    meets.event RECORD
                            │
            ┌───────────────┼───────────────┐
            ▼               ▼               ▼
        start_date      end_date      room_location_ids
            │               │               │
            └───────┬───────┴───────┬───────┘
                    │               │
      ┌─────────────┴───────────────┘
      │
      │     PLUS:
      │     host_user_id
      │     virtual_room_id  
      │     zoom_link
      │
      ▼
┌─────────────────────────────────────────┐
│  @api.depends(                          │
│   'start_date'                          │
│   'end_date'                            │
│   'room_location_ids'                   │
│   'host_user_id'                        │
│   'virtual_room_id'                     │
│   'zoom_link'                           │
│  )                                      │
└────────────┬────────────────────────────┘
             │
      ┌──────▼──────────────┐
      │ ANY CHANGE ABOVE?   │
      │ YES → RECALCULATE   │
      │ NO  → USE CACHED    │
      └────────┬────────────┘
               │
      ┌────────▼─────────────┐
      │ TRIGGER COMPUTE      │
      │ METHOD:              │
      │ _compute_multi_      │
      │ timezone_display()   │
      └────────┬─────────────┘
               │
        ✅ FIELD UPDATED
```

---

## Integration Points

```
┌─────────────────────────────────────────────────────────────┐
│            Odoo Core Components                             │
└─────────────────────────────────────────────────────────────┘
         │                      │                    │
         ▼                      ▼                    ▼
    ┌─────────┐           ┌──────────┐        ┌────────────┐
    │   ORM   │           │ Forms    │        │  Activity  │
    │ Models  │           │ Views    │        │  System    │
    │ (Fields)│           │ (XML)    │        │(Tracking)  │
    └────┬────┘           └────┬─────┘        └─────┬──────┘
         │                     │                    │
         ▼                     ▼                    ▼
    ┌─────────────┐  ┌────────────────┐  ┌────────────────────┐
    │ multi_      │  │ Separator +    │  │ _regenerate_all_   │
    │ timezone_   │  │ Field display  │  │ activities()       │
    │ display =   │  │ in forms       │  │                    │
    │ fields.Html │  │                │  │ Injects table into │
    │             │  │ • meeting_     │  │ activity notes     │
    │ compute=    │  │   event_view   │  │                    │
    │ '_compute...'  │ • view.xml     │  │ Called on confirm/ │
    │             │  │                │  │ state change       │
    │ store=True  │  │ Related field  │  │                    │
    │             │  │ syncs data     │  │ Updates activity   │
    └─────────────┘  │                │  │ when meeting       │
                     │                │  │ changes            │
                     └────────────────┘  └────────────────────┘
```

---

## Comparison: Before vs After Implementation

### BEFORE 😵
```
User opens Meeting Event form:
• Sees start_date: 2024-02-08 02:00:00 UTC
• Sees end_date:   2024-02-08 04:00:00 UTC
• Sees rooms: Jakarta, Makassar (has tz field)
• Question: "So what time is it in each location?"
• Action: Opens calculator or timezone converter
• Result: Wasted time, potential errors
```

### AFTER 🎉
```
User opens Meeting Event form:
• Sees nice table:
  ┌────────────────────┬─────────────────────────┐
  │ Location           │ Local Time              │
  ├────────────────────┼─────────────────────────┤
  │ 🏢 Ruangan Jakarta │ 09:00 - 11:00 (Jkt)    │
  │ 🏢 Ruangan Makassar│ 10:00 - 12:00 (Mks)    │
  │ 🎥 Zoom (Host)     │ 09:00 - 11:00 (Jkt)    │
  └────────────────────┴─────────────────────────┘
• Understanding: Immediate! All times visible
• Action: Can invite confidently
• Result: Perfect coordination, no confusion
```

---

## Performance Notes

```
CALCULATION PATH:
1. User creates/edits meeting.event
2. Saves record → Triggers @api.depends
3. _compute_multi_timezone_display() runs
4. Generates HTML string
5. Stored in DB (store=True)

BENEFITS:
✓ Fast form loads (no real-time computation)
✓ HTML cached in database
✓ Reused across all views
✓ Activity notifications don't recompute

TRADE-OFF:
• Uses ~2KB storage per record
• Acceptable for production use
```

---

**Architecture Status:** ✅ CLEAN, EFFICIENT, PRODUCTION-READY
